type Query {
  getGitHubRepositoriesByOrg(
    org: ID!
    sort: String
    direction: String
  ): GitHubOrganizationRepositories
    @cacheControl(scope: PUBLIC, maxAge: SHORT)
    @SetupControllers

  task(id: ID!): Task @CheckSchemas @CheckStoreUser @SetupControllers

  tasks(input: SearchInput): TaskSearchResult!
    @CheckSchemas
    @CheckStoreUser
    @SetupControllers
}

type Mutation {
  deleteTask(id: ID!): Task @CheckSchemas @CheckStoreUser @SetupControllers

  saveTask(id: ID, title: String!, description: String!): Task!
    @CheckSchemas
    @CheckStoreUser
    @SetupControllers
}
